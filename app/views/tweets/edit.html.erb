<div class="main">
    <div class="form-wrapper">
    <%#= form_with(model: @tweet, url: tweet_path(@tweet.id), local: true) do |form| %>
      <h2 class="page-heading">グルメ投稿編集</h2>
      <%= render partial: "form", locals: { tweet: @tweet } %>
    <%# end %>
    </div>
</div>